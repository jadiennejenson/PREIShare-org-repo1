# Investor listing types

This directory is reserved for shared TypeScript models used by PREIshare investor listing features.

Keep reusable domain shapes here instead of redefining them in fixtures or UI modules. Validate external data at runtime before treating it as one of these types.
